NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional. Server-only, bypasses RLS. Lets scripts/run-sql.mjs apply
# supabase/*.sql migrations directly instead of pasting them into the
# dashboard's SQL Editor. Get it from Settings -> API -> Secret keys.
# Never commit the real value.
SUPABASE_SERVICE_ROLE_KEY=
